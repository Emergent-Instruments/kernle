{
  "name": "kernle",
  "version": "0.1.0",
  "displayName": "Kernle Memory",
  "description": "Stratified persistent memory for synthetic intelligences. Replaces native memory with Kernle's structured memory system.",
  "entry": "dist/index.js",
  "hooks": [
    "before_agent_start",
    "agent_end",
    "before_tool_call",
    "tool_result_persist"
  ],
  "config": {
    "stackId": {
      "type": "string",
      "description": "Kernle stack ID. If not set, auto-detected from agent session key or KERNLE_STACK_ID env var.",
      "required": false
    },
    "tokenBudget": {
      "type": "number",
      "description": "Token budget for memory loading",
      "default": 8000
    },
    "timeout": {
      "type": "number",
      "description": "Timeout in ms for kernle CLI calls",
      "default": 5000
    },
    "kernleBin": {
      "type": "string",
      "description": "Path to kernle binary",
      "default": "kernle"
    }
  }
}
